<form method="post" action="{{ path('app_destination_delete', {'id_destination': destination.id_destination}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destination.id_destination) }}">
    <button class="btn">Delete</button>
</form>
