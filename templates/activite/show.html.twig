{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ activite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.description }}</td>
            </tr>
            <tr>
                <th>Budget</th>
                <td>{{ activite.budget }}</td>
            </tr>
            <tr>
                <th>Niveaudifficulte</th>
                <td>{{ activite.niveaudifficulte }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ activite.lieu }}</td>
            </tr>
            <tr>
                <th>Agemin</th>
                <td>{{ activite.agemin }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ activite.statut }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ activite.duree }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ activite.imagePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activite_index') }}">back to list</a>

    <a href="{{ path('app_activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('activite/_delete_form.html.twig') }}
{% endblock %}
